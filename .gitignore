node_modules/
dist/
.DS_Store
*.local
.env
.env.local
.env.*.local
!.env.example
tsconfig.tsbuildinfo

# Custom TLS (generated on server — never commit private keys)
tls/*.key
tls/*.csr
tls/*.srl
tls/argus-ca.key

# ESPHome local build + secrets (use secrets.yaml.example)
deploy/esphome/.esphome/
deploy/esphome/secrets.yaml
