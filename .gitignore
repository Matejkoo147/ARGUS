node_modules/
dist/
.DS_Store
*.local
.env
.env.local
.env.*.local
!.env.example
tsconfig.tsbuildinfo

# Cursor IDE — local hooks & agent state (personal, not shared)
.cursor/

# Custom TLS (generated on server — never commit private keys)
tls/*.key
tls/*.csr
tls/*.srl
tls/argus-ca.key

# Local secrets (if any)