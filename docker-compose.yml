services:
  argus:
    build:
      context: .
      dockerfile: Dockerfile
    image: argus-home-security:latest
    restart: unless-stopped
    ports:
      # Bind only on WireGuard IP — not exposed on LAN/public (see DEPLOY.md)
      - "${ARGUS_BIND_IP:-10.8.0.1}:${ARGUS_PORT:-9080}:8080"
    environment:
      TZ: ${TZ:-Europe/Bratislava}
      # Optional: proxy Home Assistant through ARGUS (avoids CORS). Example:
      # ARGUS_HA_UPSTREAM=http://192.168.1.50:8123
      ARGUS_HA_UPSTREAM: ${ARGUS_HA_UPSTREAM:-}
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/"]
      interval: 30s
      timeout: 5s
      retries: 3
