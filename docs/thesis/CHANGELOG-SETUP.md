# Setup changelog (chronological)

Append every real step. Short entries are fine.

## 2026-08-05 — Planning

- Defined target: Pi 5 16 GB + active cooler + Touch Display 2 landscape; HA + ARGUS on Pi; Ollama on mato-server only.
- Created `docs/thesis/` living documentation for later PDF export.
- **Locked Option B:** Raspberry Pi OS + Docker (HA Container + ARGUS + landscape kiosk).
- **Locked first install:** Network Install (Shift) onto microSD already in the Pi — no PC reader / USB SSD yet; buy SSD later for durability.
- Prior stack: ARGUS + HA on mato-server; UI through v1.0.5+.

## Next actions

1. Assemble Pi + cooler + Touch Display 2 + keyboard + Ethernet + microSD.
2. Network Install → Raspberry Pi OS 64-bit Desktop; enable SSH; record IP/hostname here.
3. Photograph installer + first boot (`06-photo-checklist.md`).
4. Landscape rotation → verify ~1280×720.
5. Docker + HA + ARGUS; Ollama upstream = mato-server.
6. Later: migrate boot/data to USB SSD when purchased.
